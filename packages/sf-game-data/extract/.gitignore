# The CUE4Parse clone is not vendored — clone it here per README.md.
/CUE4Parse/

# Throwaway probe output (kept locally, never committed). The fg-probe copy
# itself now lives under /tools/ (ignored at the repo root).
probe-*.txt

# The extractor's default output (OUT=sf-game-data.json) lands beside the tool;
# the real dataset is installed into data/<channel>/ by hand, never committed here.
sf-game-data.json

# .NET build output.
**/bin/
**/obj/

# Oodle native library downloaded at runtime.
*.dll
